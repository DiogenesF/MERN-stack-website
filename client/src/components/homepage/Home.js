import React from "react";
import Carousel from "./Carousel";
import Header from "./Header";

const Home = (props) => {
  return (
    <div>
      <Header />
      <Carousel />
    </div>
  );
};

export default Home;
